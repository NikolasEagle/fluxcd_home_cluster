apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: frigate
  namespace: default
spec:
  valuesFrom:
    - kind: ConfigMap
      name: frigate-config
      valuesKey: values.yaml
